import { primary } from '@/tailwind.config';

export const bg_container: string =
  'overflow-clip  border-1 border-white\n' +
  'border-opacity-10 bg-white bg-opacity-50 px-1 py-12 backdrop-blur-3xl rounded-2xl';

export const sh_container: string =
  'shadow-[0_8px_5px_' + primary.DEFAULT.toString() + '11]';
